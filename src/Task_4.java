public class Task_4 {
    public static void main(String[] args) {
        for (int count = 10; count >= 0; count = count - 2) // sets initial value of count to 10. Sets min value to 0. Subtracts 3 to count everytime.
        {
            System.out.print(count + " "); // outputs count
        }
    }
}
