public class Task_2 {
    public static void main(String[] args) {
        for (int count = 30; count >= 0; count--) // sets initial value of count to 30. Sets min value to 0. Subtracts 1 to count everytime.
        {
            System.out.print(count + " "); // outputs count
        }
    }
}
